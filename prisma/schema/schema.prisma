generator client {
  provider     = "prisma-client"
  output       = "../generated/prisma"
  moduleFormat = "cjs"
  // previewFeatures = ["prismaSchemaFolder"] 
}

datasource db {
  provider = "postgresql"
}
